## 📅 Week 2-3: Priority Queue (우선순위 큐) & Heap - 우선순위 기반 설계

### 1. 핵심 개념 리마인드
* **우선순위 큐(Priority Queue)**: 들어온 순서와 관계없이 우선순위가 높은 데이터가 먼저 추출되는 구조.
* **힙(Heap)**: 우선순위 큐를 효율적으로 구현하기 위한 **완전 이진 트리**. "부모는 항상 자식보다 작거나 같다(Min Heap)"는 규칙을 유지함.
* **느슨한 정렬(Weak Ordering)**: 전체를 정렬하는 대신, 부모-자식 간의 서열만 유지하여 최솟값을 $O(\log n)$만에 찾아내는 효율성을 극대화함.



### 2. 주요 로직 구현 포인트
* **저장소 선택 (ArrayList)**: 힙은 빈틈없이 채워지는 완전 이진 트리이므로, 인덱스 계산(`(i-1)/2` 등)만으로 부모/자식 노드를 즉시 찾을 수 있는 배열 기반 리스트가 최적임.
* **주요 메서드**:
    - `add(E e)`: 데이터를 맨 끝에 추가한 후, 부모와 비교하며 위로 올림 (**siftUp**).
    - `poll()`: 루트(최솟값)를 반환하고, 맨 뒤의 데이터를 루트로 옮긴 뒤 자식들과 비교하며 아래로 내림 (**siftDown**).
    - `siftDown`: 자식들 중 **더 작은 값**을 선택해 비교함으로써 최소 힙의 규칙을 재정립함.
* **비교 로직 (Comparable)**: 객체 간의 비교를 위해 `compareTo`를 사용. 부등호 방향(`.compareTo(B) < 0` $\rightarrow$ `A < B`)을 통해 로직의 직관성을 높임.



### 3. 시간 복잡도 분석 ($O$)
| 연산 | 복잡도 | 이유 |
| :--- | :--- | :--- |
| **add** | $O(\log n)$ | 최악의 경우 트리의 높이만큼 부모와 비교하며 상승 |
| **poll** | $O(\log n)$ | 루트 교체 후 트리의 높이만큼 자식과 비교하며 하강 |
| **peek** | $O(1)$ | 루트 노드(인덱스 0)를 즉시 조회 |

### 💡 Insight
"힙은 전체를 정렬하지 않아도 최솟값을 보장할 수 있다는 점에서 '효율적인 타협'의 미학을 보여준다. 특히 삭제 과정에서 발생하는 '낙하산 인사(마지막 노드의 이동)'와 '서열 정리(siftDown)' 로직을 통해, 복잡한 트리 구조를 배열 인덱스 연산만으로 완벽하게 제어할 수 있다는 점이 인상적이었다."